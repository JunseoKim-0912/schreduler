# 엄격한 스케쥴 관리 앱 — 기획 보고서 (Design Report) v3

- **최초 작성일:** 2026-09-16
- **v2:** June의 결정 사항 반영 (앱 형태, 에스컬레이션, 페르소나 초안, 포인트 공식 등)
- **v3:** LLM 인퍼런스 비용 최적화 전략 4가지 반영, 한/영(KOR/ENG) 다국어 지원 요건 추가
- **v3.6 (2026-09-25):** FR-2를 자연어 일정 관리(생성·삭제·수정) + 되돌리기로 확장, `ActionHistory` 테이블 추가
- **작성 대상 (Owner):** June (Junseo Kim)

> **v3 변경 요약:** 프롬프트 캐싱 도입, FR-6에 LLM 우회 UI 숏컷 결합, LLM 라우팅 전략 확정(MVP는 API만 사용 → 이후 하이브리드 전환 검토), FR-8 컨텍스트 동적 로딩, 신규 FR-11(한/영 다국어 지원) 추가.

---

## 1. 프로젝트 개요

이 앱은 단순 일정 기록용 캘린더가 아니라, **"계획을 지키게 강제하는" 스케쥴 관리 시스템**이다. 세 가지 축으로 기존 캘린더 앱과 차별화된다.

1. **자연어 인터페이스** — 사용자가 대화하듯 일정을 말하면 LLM이 부족한 정보를 되물어 채운다.
2. **엄격한 실행 관리 (Enforcement)** — 일정 시작/종료 시점에 알림을 보내고, 완료 체크를 직접 하게 만들며, 어겼을 경우 사유를 기록해 피드백에 활용한다.
3. **정서적 완충 장치 + 게이미피케이션** — 가상 캐릭터와의 대화로 부담을 낮추고, 규칙적인 생활을 포인트로 보상한다.

**아키텍처 원칙:** 백엔드(캘린더, LLM 연동, 알림 스케쥴링, DB)는 전부 Python(FastAPI)으로 구현한다. 클라이언트(모바일 앱 UI)는 Flutter 또는 React Native로 별도 구현하고 Python 백엔드와 REST/WebSocket API로 통신한다. **한국어/영어 두 언어를 모두 지원**한다 (FR-11).

---

## 2. 핵심 기능 명세 (Functional Requirements)

### FR-1. 캘린더 코어

이벤트의 기본 속성(날짜, 시작 시각, 지속 시간, 제목/설명), 단발성/반복 이벤트 지원, 월/주/일 뷰. (변경 없음)

**✅ v3.4 추가 — 마감일(Deadline) 이벤트 타입:** 과제 마감일처럼 "시작 시각"이 의미 없고 "언제까지"만 있는 항목을 위해, `Event.event_type` 필드(`scheduled` / `deadline`)를 추가한다. `scheduled`(기본값)는 기존처럼 `start_time`/`end_time`이 모두 필요한 시간대 이벤트이고, `deadline`은 `start_time`이 `null`이며 `end_time` 하나만 "마감 일시"로 쓴다. 새 테이블을 만들지 않고 기존 `Event`를 재사용하므로, 반복(`recurrence_rule`), 중요도, Child 이벤트(FR-5), 미준수 사유(FR-6), 포인트(FR-10) 로직을 그대로 물려받는다 (예: "매주 금요일까지 제출하는 과제"도 반복 마감일로 표현 가능). FR-4 알림만 `event_type`에 따라 분기한다 (상세: FR-4 참고).

**✅ v3.5 추가 — Task List (할 일 목록) 편의 API:** 사용자가 마감일 이벤트들을 "할 일 목록" 형태로 따로 조회/관리하고 싶어해서, 별도 테이블 없이 `event_type="deadline"`인 `Event`/`EventInstance`를 대상으로 하는 편의 API(`/tasks`)를 추가한다. Task는 개념적으로 deadline 이벤트와 동일한 객체이므로 알림·에스컬레이션·미준수 리포트·포인트 적립이 전부 그대로 적용된다 — "가벼운 체크리스트"가 아니라 "마감일 이벤트를 목록 뷰로 보여주는 것"에 가깝다. `GET /tasks`(마감일 오름차순, 완료 여부·overdue 여부 포함), `POST /tasks`(deadline 이벤트 생성 축약형), `PUT /tasks/{event_instance_id}/complete`(완료 처리 축약형) 세 엔드포인트로 구성한다.

### FR-2. LLM 기반 자연어 이벤트 추가

사용자가 자연어로 일정을 말하면 LLM이 부족한 슬롯만 되물어 채운다. **중요 기간(Important Date Range)** 개념으로 "학기", "인턴십" 등 임의의 기간을 등록해두고 반복 종료 기준으로 재사용한다 (v2에서 확정, "공강까지" 같은 모호한 표현은 항상 명시적 확인으로 해소).

**✅ v3 추가 — LLM 라우팅 전략:** 열린 질문이었던 "로컬 모델 vs API" 중 **MVP 단계는 API만 사용**하기로 확정했다 (저비용 API 모델 1종, 예: Claude Haiku 계열 또는 GPT-4o-mini 계열). 슬롯필링용 로컬 sLLM 자체호스팅은 1인 프로젝트 단계의 서버·GPU·업타임 관리 부담을 고려해 지금은 도입하지 않고, 이후 사용량이 늘어 API 비용이 부담되는 시점에 슬롯필링(FR-2)만 로컬 모델로 옮기는 하이브리드 전환을 재검토한다. (상세: [9. LLM 인퍼런스 비용 최적화 전략](#9-llm-인퍼런스-비용-최적화-전략-v3-신규))

**✅ v3.6 추가 — 자연어 일정 관리 (생성 + 삭제 + 수정) + 되돌리기:** 원래 FR-2는 "새 일정 추가" 전용 슬롯필링이었지만, 실사용 중 "물리 퀴즈 없애줘" 같은 삭제 요청을 넣었을 때 LLM이 이를 새 이벤트 생성으로 착각해 엉뚱한 질문을 이어가는 문제가 발견됐다. 이에 자연어 입력을 **일정 관리 전반**으로 확장한다.

- **의도 분류:** LLM이 먼저 발화의 의도를 `create` / `delete` / `update` / `unknown` 중 하나로 분류한다. `create`는 기존 슬롯필링 흐름을 그대로 탄다. `unknown`이면 "일정 추가·삭제·수정만 도와드릴 수 있어요" 같은 안내를 돌려주고 엉뚱한 슬롯을 묻지 않는다.
- **대상 찾기는 백엔드가 결정:** LLM은 "어떤 이벤트인지에 대한 설명"(제목 키워드, 날짜, "전부" 여부)만 구조화해서 내고, 실제 DB에서 어떤 이벤트/인스턴스를 가리키는지는 **백엔드가 결정적으로 매칭**한다. LLM이 이벤트 ID를 직접 고르게 하지 않는다 (환각으로 엉뚱한 이벤트를 지우는 걸 막기 위함). 후보가 0개면 "해당 일정을 찾지 못했어요", 2개 이상이면 후보 목록을 보여주고 되묻는다.
- **반복 이벤트의 범위:** "오늘 물리 퀴즈"처럼 날짜가 특정되면 **그 날짜의 `EventInstance` 하나만** 수정/삭제하고, "물리 퀴즈 전부"처럼 시리즈 전체를 가리키면 `Event` 전체에 적용한다. 반복 이벤트인데 범위가 불분명하면 "이번만 / 반복 전체" 중 어느 쪽인지 되묻는다.
- **수정 규칙:** 시작 시각만 바꾸라고 하면 기존 지속 시간을 유지한 채 종료 시각도 같이 민다 (예: 5–6시 → 6–7시). 종료 시각을 명시하면 그 값을 쓴다.
- **실행 전 확인은 위험도에 따라:** 이벤트 한 개에 대한 삭제/수정은 바로 실행하고 "되돌리기"로 복구할 수 있게 한다. **2개 이상을 한 번에 지우거나 바꾸는 요청**(예: "전부 없애줘")은 영향받는 목록과 개수를 먼저 보여주고 사용자 확인 후 실행한다.
- **되돌리기(Undo):** 자연어로 실행된 모든 변경(생성·삭제·수정)은 `ActionHistory`에 **변경 전 스냅샷**과 함께 기록한다. 사용자가 "되돌리기"를 누르면 스냅샷으로 원래 상태(원래 ID, 연관된 `EventInstance`·Child 이벤트 포함)를 복원하고, 알림 잡도 다시 등록한다. 과거 날짜 인스턴스가 영향을 받았다면 포인트 원장도 다시 계산한다(v3.5의 늦은 완료 처리와 같은 재계산 로직 재사용). 되돌리기는 LLM이 사용자의 의도와 다르게 동작했을 때의 안전장치다.
- 모든 변경은 기존 서비스 함수(이벤트 생성/수정/삭제 로직)를 거쳐 실행해서, 알림 재등록·Child 이벤트 동기화 등 기존 부수효과가 그대로 적용되게 한다.

### FR-3. 중요도(Importance) 체계

[3. 중요도 체계](#3-중요도-importance-체계) 참고. (변경 없음)

### FR-4. 출석/수행 확인 알림 (Strict Enforcement)

이벤트 시작/종료 시점 앱 푸시 알림(FCM), 사용자 동의 시 GPS 자동 확인 병행. 완료 체크는 사용자가 직접. (v2에서 확정, 변경 없음)

**✅ v3.4 추가 — 마감일 이벤트 알림 분기:** `Event.event_type`이 `deadline`인 경우 시작 알림은 생성하지 않는다. 대신 (1) 마감(`end_time`) 1일 전 리마인더 알림, (2) 마감 시각의 완료 여부 확인 알림, 이렇게 두 개만 생성한다. `scheduled` 타입 이벤트는 기존처럼 시작/종료 알림 두 개를 그대로 유지한다.

#### FR-4-1. 통합 에스컬레이션 정책

개별 이벤트 무응답과 앱 전체 미접속에 동일한 규칙 적용: 1주(텔레그램 opt-in 알림) → 2주(모든 알림 중단) → 3주(최종 메시지 후 완전 중단), 응답 시 즉시 리셋. (v2에서 확정, 변경 없음)

### FR-5. Child 이벤트

부모 이벤트의 이동/준비 시간을 하위 이벤트로 자동 생성, 중요도 상속. 초기엔 사용자가 장소별 이동시간을 수동 등록. (v2에서 확정, 변경 없음)

### FR-6. 미준수 시 사유 조사 + 피드백 DB

카테고리 선택(필수) + 자유 텍스트(선택, ≤150자)의 2단계 입력. 카테고리: `overslept`, `fatigue`, `priority_shift`, `schedule_conflict`, `forgot`, `transit_issue`, `other`.

**✅ v3 추가 — LLM 우회 UI 숏컷:** 사용자가 카테고리 버튼(`overslept`, `fatigue` 등)만 누르고 끝내면 **LLM을 전혀 호출하지 않고 바로 `ComplianceReport` 테이블에 저장**한다. LLM 호출(사유 분석·페르소나의 공감/피드백 반응 생성)은 사용자가 `other`를 선택했거나, 카테고리 선택 후 자유 텍스트를 추가로 입력했을 때만 트리거된다. 즉 "버튼 클릭만으로 끝나는 대다수 케이스"는 인퍼런스 비용이 0이다.

### FR-7. 실제 수면 시간 체크

기상 직후 어제 취침/오늘 기상 시각 질문, 실제 수면 패턴 기록. (변경 없음)

### FR-8. 매일 밤 9시 하루 요약 체크인

오후 9시 체크인으로 "실제 하루" 캘린더를 계획 캘린더와 별도 레이어로 저장.

**✅ v3 추가 — 컨텍스트 동적 로딩:** 체크인 시점에 하루 전체 `EventInstance` 로그를 통째로 LLM에 넘기지 않는다. 백엔드가 미리 상태를 판별해:
- **완료(`done`) 이벤트:** 개수만 요약 (예: "오늘 계획한 6개 중 5개 완료").
- **미완료(`missed`) 이벤트:** 제목·시간·[FR-6]에서 기록된 미준수 사유까지 상세히 프롬프트에 포함.

이렇게 하면 LLM이 굳이 다 알 필요 없는 "잘 지킨 일정"에 토큰을 쓰지 않고, 대화가 필요한 "놓친 일정"에만 컨텍스트를 집중시킬 수 있다.

### FR-9. 가상 AI 캐릭터를 통한 대화형 인터페이스 (플래그십 기능)

사전 정의된 페르소나 3~5개(초안 4종: 코치 강한결/다정한 유리/선배 정우/말랑이), JSON으로 DB 저장 후 시스템 프롬프트로 주입. (v2에서 확정)

**✅ v3 추가 — 프롬프트 캐싱:** 페르소나 설정(`tone_description`, `sample_lines` 등 고정된 부분)과 사용자의 중요 기간(Important Date Range) 목록처럼 자주 재사용되지만 자주 바뀌지 않는 컨텍스트는 **API 프롬프트 캐싱**(Claude/OpenAI가 지원하는 기능)을 이용해 캐시에 올려둔다. 매 호출마다 이 고정 정보를 새로 입력 토큰으로 계산하지 않아도 되므로, 대화가 잦은 페르소나 인터랙션에서 입력 토큰 비용을 크게 절감할 수 있다. (설계 시 유의점: 캐시는 "완전히 동일한 프리픽스"일 때 효과가 크므로, 페르소나 JSON과 중요 기간 목록처럼 자주 안 변하는 내용을 프롬프트 앞부분에 고정 배치하고, 매번 달라지는 사용자 발화·이벤트 정보는 뒤쪽에 붙이는 구조로 설계.)

**✅ v3 추가 — 다국어 페르소나 (FR-11 연동):** 각 페르소나의 `display_name`, `tone_description`, `sample_lines`는 언어별로 분리 저장한다.

```json
{
  "persona_id": "coach",
  "display_name": { "ko": "코치 강한결", "en": "Coach Kang" },
  "tone_description": {
    "ko": "직설적이고 절도 있는 반말, 강한 동기부여형",
    "en": "Blunt, disciplined, highly motivational tone"
  },
  "sample_lines": {
    "event_start_check": {
      "ko": "지금 시간이면 벌써 가 있어야죠! {event_title} 가셨어요?",
      "en": "You should already be there! Did you make it to {event_title}?"
    }
  }
}
```

LLM 호출 시 `User.preferred_language` 값을 시스템 프롬프트에 명시해 해당 언어로만 응답하도록 강제한다.

**✅ v3.2 추가 — description 기반으로 스키마 단순화 (개발자가 하드코딩 없이 페르소나 생성 가능):** 위 예시처럼 `tone_description`/`sample_lines`를 세부 필드로 쪼개는 대신, **자유 텍스트 `description` 필드 하나**에 페르소나의 성격·말투·톤을 한 문단으로 서술하고, 이를 대화 시 시스템 프롬프트에 그대로 삽입하는 방식으로 단순화한다 (언어별로 필요하면 `description`을 언어별로 하나씩 저장). 이 방식의 장점: (1) 개발자가 Python 코드를 건드리지 않고 DB에 row 하나(이름 + 설명 문장)만 추가해도 새 페르소나가 즉시 동작한다. (2) Persona CRUD API(`POST/GET/PUT/DELETE /personas`, 9주차 구현)를 그대로 관리자 화면이나 프론트엔드 폼에 연결하면, 비개발자도 "이름 + 설명" 입력만으로 페르소나를 만들 수 있어 향후 확장성이 좋다.

**✅ v3.3 추가 — 예시 대사 + 배경 스토리 (few-shot으로 캐릭터 일관성 강화):** 플래그십 기능인 만큼, `description` 한 문단만으로는 대화가 여러 턴 이어질 때 캐릭터 톤이 흔들릴 수 있다. 이를 보완하기 위해 두 개의 선택적 필드를 추가한다.
- `example_lines` (언어별): `{ "situation": "...", "line": "..." }` 형태의 대표 상황별 실제 대사 3~5개 (예: "사용자가 지각했을 때", "목표를 달성했을 때", "반복해서 놓쳤을 때"). LLM 프롬프트 엔지니어링에서 말하는 few-shot 예시 역할을 해서, description만 줄 때보다 캐릭터의 말투가 대화 내내 훨씬 안정적으로 유지된다.
- `backstory` (언어별): 캐릭터가 왜 그런 성격을 갖게 됐는지 설명하는 짧은 배경 이야기 한 문단. 캐릭터의 "왜"까지 LLM에 제공해 반응의 일관성과 개연성을 높인다.

두 필드 모두 `description`과 함께 시스템 프롬프트 앞부분(자주 안 바뀌는 부분)에 배치되어, 9주차 Day 3의 프롬프트 캐싱 대상에 그대로 포함된다.

```json
{
  "name": "rordon",
  "display_name": { "ko": "로든 갬지", "en": "Rordon Gamsay" },
  "description": { "ko": "...", "en": "..." },
  "example_lines": {
    "ko": [
      { "situation": "사용자가 지각했을 때", "line": "지각하는 너를 기다리자니 차라리 남극에서 웰던 스테이크를 구워먹는 게 더 빠르겠다." }
    ],
    "en": [
      { "situation": "User is late", "line": "It would be faster to eat a well-done steak at the South Pole than to wait for you here." }
    ]
  },
  "backstory": {
    "ko": "한때 미슐랭 별을 노리던 주방에서 완벽을 요구받으며 일했다...",
    "en": "..."
  }
}
```

### FR-10. 규칙성 기반 포인트 (게이미피케이션)

이벤트 점수 = 중요도 가중치 × 완료 여부, 연속 달성 streak 보너스(3일 ×1.1, 7일 ×1.25, 14일 ×1.5). (v2에서 확정 초안, 변경 없음)

### FR-11. 한국어/영어 다국어 지원 (v3 신규)

June의 요구: "앱은 먼저 한/영 사이에서 사용할 수 있게" — 즉 서비스 시작 시점부터 한국어·영어 두 언어를 모두 지원한다.

- `User.preferred_language` (`ko` / `en`) 필드로 사용자별 선호 언어를 저장.
- **클라이언트 UI:** Flutter의 표준 i18n 패키지(`intl`, `easy_localization` 등)로 모든 고정 문자열(메뉴, 버튼, 카테고리 라벨 등)을 리소스화.
- **페르소나 대화(FR-9):** 위 예시처럼 `sample_lines` 등을 언어별로 분리 저장, LLM 프롬프트에 응답 언어 명시.
- **알림 텍스트(FR-4, FR-4-1):** 푸시/텔레그램 알림 문구도 언어별 템플릿으로 관리.
- **FR-6 카테고리 라벨:** `overslept` 같은 코드값은 그대로 두고, 화면에 노출되는 라벨만 `{ "ko": "늦잠/기상 실패", "en": "Overslept" }` 식으로 이중 저장.
- (참고) LLM에게 자유 텍스트를 분석시키는 경우(FR-6의 `other`, FR-8 체크인 등) 입력 언어를 자동 감지하거나 `preferred_language`를 참고해 같은 언어로 응답하도록 지시.

---

## 3. 중요도(Importance) 체계

| 값 | 분류 | 설명 / 예시 |
|---|---|---|
| **없음 (N/A)** | 수면 | 중요도 자체가 없는 특수 카테고리. |
| **1** | 개인 여가 | 예: 매일 6:00–6:30 독서 |
| **2** | 타인 연관 약속 | 친구와의 약속, 모임 |
| **3** | 의무이지만 출석 체크 없음 | 출석 안 부르는 강의 |
| **4** | 공식적 의무/평가 | 퀴즈, 공식 모임 |
| **5** | 반드시 지켜야 함 | 미드텀, 파이널 |
| **MAX** | 예외적 최상위 | 5보다 더 중요한 긴급 이벤트 |

---

## 4. 데이터 모델 (v3 업데이트)

| 테이블 | 주요 필드 | 비고 |
|---|---|---|
| `User` | id, name, selected_persona_id, preferred_language(`ko`/`en`), telegram_chat_id, telegram_opt_in | **v3:** `preferred_language` 추가 |
| `ImportantDateRange` | id, user_id, name, start_date, end_date | 학기/인턴십 등 범용 기간 |
| `Event` | id, user_id, title, event_type(`scheduled`/`deadline`), start_time(deadline이면 null), end_time, importance, is_recurring, recurrence_rule, date_range_id, parent_event_id | **v3.1:** `recurrence_rule`은 자체 한글 DSL이 아니라 **RFC5545 RRULE 형식**으로 저장(예: `FREQ=WEEKLY;BYDAY=MO`, `FREQ=WEEKLY;BYDAY=TU,TH`). 날짜 생성/파싱은 직접 구현하지 않고 `python-dateutil`의 `rrule`을 사용한다. FR-2 슬롯필링에서도 LLM이 자연어를 직접 이 문자열로 만들게 하지 말고, frequency·요일 코드 같은 구조화된 슬롯을 채우게 한 뒤 백엔드에서 RRULE 문자열로 조립한다. **v3.4:** `event_type` 추가 — `deadline`이면 `start_time`은 null, `end_time`을 마감 일시로 사용(예: 과제 마감일). 반복/중요도/Child/포인트 로직은 기존 `Event` 그대로 재사용, FR-4 알림만 분기(마감 1일 전 리마인더 + 마감 시각 완료 확인). |
| `EventInstance` | id, event_id, date, status, completion_method | |
| `EngagementState` | id, user_id, scope, ref_event_id, last_response_at, escalation_stage, stage_updated_at | |
| `ComplianceReport` | id, event_instance_id, reason_category, reason_text(≤150자), llm_triggered(bool), created_at | **v3:** `llm_triggered` 추가 — 버튼만으로 끝났는지, LLM이 개입했는지 기록해 비용 추적에 활용 |
| `SleepLog` | id, user_id, date, actual_bedtime, actual_wake_time | |
| `DailyActualLog` | id, user_id, date, summary_text, actual_events(JSON) | |
| `Persona` | id, name, display_name(JSON, 언어별), description(자유 텍스트, 언어별), example_lines(JSON, 언어별 상황+대사 배열, optional), backstory(자유 텍스트, 언어별, optional) | **v3.2:** 세부 필드 여러 개 대신 자유 텍스트 `description` 하나로 단순화 — 개발자가 코드 변경 없이 DB row 추가만으로 새 페르소나 생성 가능. CRUD API(`/personas`)를 통해 향후 프론트엔드/관리자 화면에서도 생성 가능하도록 설계. **v3.3:** `example_lines`/`backstory` 추가 — few-shot 예시로 캐릭터 일관성 강화 |
| `PersonaConversation` | id, user_id, persona_id, context_type, messages(JSON) | |
| `PointsLedger` | id, user_id, date, base_points, streak_multiplier, points_earned | |
| `ActionHistory` | id, user_id, action_type(`create`/`delete`/`update`), source(`nl`/`ui`), summary_text, snapshot_before(JSON), affected_ids(JSON), created_at, undone_at(nullable) | **v3.6:** 자연어(및 목록의 삭제 버튼)로 실행된 변경의 되돌리기용 기록. `undone_at`이 채워지면 이미 되돌린 항목. |

---

## 5. 시스템 아키텍처

```
┌───────────────────────────────┐
│   클라이언트: Flutter/RN 앱      │   ← 네이티브 모바일, 푸시 수신(FCM), GPS, 다국어 UI(FR-11)
└───────────────┬───────────────┘
                │ REST/WebSocket API
┌───────────────▼───────────────────────────────────────────┐
│                  Python 백엔드 (FastAPI)                     │
│  ┌───────────────┐  ┌────────────────┐  ┌───────────────┐ │
│  │ 캘린더 코어      │  │ LLM 이벤트 파서    │  │ 페르소나 대화    │ │
│  │ (FR-1)         │  │ (FR-2)          │  │ (FR-9, 캐싱 적용) │ │
│  └───────────────┘  └────────────────┘  └───────────────┘ │
│  ┌───────────────┐  ┌────────────────┐  ┌───────────────┐ │
│  │ Child 이벤트    │  │ 알림/에스컬레이션  │  │ 피드백 수집      │ │
│  │ 생성기 (FR-5)   │  │ 엔진(FR-4,4-1)   │  │ (FR-6, UI 숏컷) │ │
│  └───────────────┘  └────────────────┘  └───────────────┘ │
│  ┌───────────────┐  ┌────────────────┐  ┌───────────────┐ │
│  │ 수면 체크인      │  │ 9시 체크인        │  │ 포인트 엔진      │ │
│  │ (FR-7)         │  │ (FR-8, 동적로딩)  │  │ (FR-10)       │ │
│  └───────────────┘  └────────────────┘  └───────────────┘ │
└───────┬───────────────────┬───────────────────┬───────────┘
        │                   │                   │
        ▼                   ▼                   ▼
   DB (SQLite/       FCM (앱 푸시)       텔레그램 봇 API
   PostgreSQL)                          (에스컬레이션 전용)
        │
        ▼
   저비용 LLM API (MVP)
   — 이후 슬롯필링만 로컬 sLLM으로 분리하는 하이브리드 전환 검토
```

**LLM 계층 요약 (v3):**

- **MVP:** 슬롯필링(FR-2)·페르소나 대화(FR-9)·피드백 분석(FR-6 `other`)·일일 체크인(FR-8) 모두 동일한 저비용 API 모델 1종으로 처리.
- **비용 절감 4종 전략** (상세는 [9절](#9-llm-인퍼런스-비용-최적화-전략-v3-신규)): 프롬프트 캐싱, LLM 우회 UI 숏컷, (장기) 하이브리드 라우팅, 컨텍스트 동적 로딩.
- **향후 확장:** 트래픽이 늘어 API 비용이 부담되면, 구조화된 출력이 필요한 슬롯필링(FR-2)만 자체호스팅 로컬 sLLM(예: Llama 3 8B급)으로 옮기고, 자연스러운 텍스트 생성이 중요한 페르소나 대화(FR-9)는 계속 API에 남긴다.

---

## 6. 남은 열린 질문

1. **미준수 사유 카테고리 최종안:** [FR-6](#fr-6-미준수-시-사유-조사--피드백-db)의 7개 카테고리는 초안이며 확정 필요.
2. **포인트 공식 세부 숫자:** 가중치·streak 배율은 제안값이며 확정/조정 필요.
3. **계획 캘린더 vs 실제 캘린더 비교 UI:** 클라이언트 UI 설계 단계에서 다룰 사항.
4. **GPS 지오펜싱 세부 정책:** 위치 확인 주기, 배터리 최적화 방식.
5. **페르소나 상세 프롬프트/문구 (한/영 모두):** 초안 4종의 `sample_lines`는 예시이며 실제 톤앤매너는 프롬프트 튜닝 단계에서 다듬음.
6. **하이브리드 전환 트리거 기준:** "API 비용이 부담되는 시점"을 구체적으로 어떤 지표(월 비용, 요청 수 등)로 판단해 전환할지는 서비스 운영 데이터가 쌓인 뒤 결정.
7. **마감일(deadline) 리마인더 타이밍:** 현재 "마감 1일 전" 고정값이며, 나중에 이벤트별로 조정 가능하게 확장할지는 열어둠.
8. **되돌리기 보관 범위:** 현재는 되돌리지 않은 기록을 계속 보관하고 가장 최근 것부터 되돌릴 수 있게 한다. 오래된 기록을 며칠 뒤 정리할지, 몇 단계까지 되돌릴 수 있게 할지는 실사용 후 결정.

> LLM 실행 방식(로컬 vs API)은 이번 라운드에서 "MVP는 API, 이후 하이브리드 검토"로 해소되어 목록에서 제외했다.

---

## 7. 제안 개발 로드맵

| 단계 | 범위 | 포함 기능 |
|---|---|---|
| Phase 0 | 기본 뼈대 | FastAPI 백엔드 + Flutter 클라이언트 스캐폴딩 + DB 스키마 + i18n 리소스 구조(FR-11) |
| Phase 1 (MVP) | 기본 캘린더 + 중요도 태깅 | FR-1, FR-3 (수동 입력) |
| Phase 2 | 자연어 이벤트 생성 | FR-2 (API 기반 슬롯필링, 중요 기간 등록) |
| Phase 3 | 엄격 관리 엔진 | FR-4, FR-4-1, FR-5 |
| Phase 4 | 피드백 & 실제 생활 기록 | FR-6(UI 숏컷 포함), FR-7, FR-8(동적 컨텍스트 로딩) |
| Phase 5 | 정서적 인터페이스 & 보상 | FR-9(페르소나 4종, 프롬프트 캐싱, 한/영 대사), FR-10 |
| Phase 6 | 확장/최적화 | 하이브리드 LLM 라우팅 검토, GPS 지오펜싱 고도화, 페르소나 커스텀 생성, 지도 API 연동 |

---

## 8. 기술 스택 요약

| 영역 | 제안 |
|---|---|
| 백엔드 언어/프레임워크 | Python / FastAPI |
| 클라이언트 | Flutter(Dart), i18n 패키지로 한/영 지원 |
| DB / ORM | SQLite → PostgreSQL / SQLAlchemy |
| 스케쥴링 | APScheduler → Celery+Redis(확장 시) |
| 푸시 알림 | Firebase Cloud Messaging (FCM) |
| 에스컬레이션 채널 | 텔레그램 봇 (`python-telegram-bot`, opt-in) |
| GPS/위치 | 클라이언트 위치 API + 지오펜싱 |
| LLM (MVP) | 저비용 API 모델 1종 + 프롬프트 캐싱 |
| LLM (확장 시) | 슬롯필링용 로컬 sLLM(예: Llama 3 8B급) 자체호스팅 + 페르소나 대화용 API 유지 |

---

## 9. LLM 인퍼런스 비용 최적화 전략 (v3 신규)

기존 설계에 이미 반영돼 있던 "슬롯 상태만 전달", "구조화된 흐름 제한" 같은 기본기에 더해, 다음 4가지를 확정 반영한다.

1. **프롬프트 캐싱** — 페르소나 설정(FR-9)과 중요 기간 목록처럼 자주 바뀌지 않는 컨텍스트를 API의 프롬프트 캐시에 고정 배치해 입력 토큰 비용을 절감.
2. **LLM 우회 UI 숏컷** — FR-6에서 카테고리 버튼 선택만으로 끝나는 경우 LLM을 호출하지 않고 DB에 직행, `other` 선택 또는 자유 텍스트 입력 시에만 LLM 호출.
3. **하이브리드 라우팅 (장기 전략)** — 슬롯필링(FR-2)은 로컬 sLLM, 페르소나 대화(FR-9)는 저비용 API로 분리하는 구조를 장기 목표로 두되, **MVP 단계에서는 인프라 부담을 피하기 위해 API로만 통일**하고, 트래픽이 늘어난 뒤 전환을 재검토한다 (열린 질문 #6).
4. **이벤트 상태 기반 컨텍스트 동적 로딩** — FR-8 일일 체크인 시, 완료된 이벤트는 개수만 요약하고 미완료 이벤트만 상세 정보(사유 포함)를 프롬프트에 주입.

---

*이 문서는 v2 대비 LLM 비용 최적화 전략과 다국어 지원 요건을 반영한 v3이며, 남은 열린 질문(6절)이 정리되는 대로 API 명세·DB 스키마 SQL·프롬프트 템플릿 단계로 진행할 수 있다.*