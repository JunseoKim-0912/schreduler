[pytest]
pythonpath = .
# Only collect tests/ so ad-hoc root scripts named test_*.py (which call a live server) are never run.
testpaths = tests
# Allow the same test file name in different folders (tests/test_points.py and tests/services/test_points.py).
addopts = --import-mode=importlib
